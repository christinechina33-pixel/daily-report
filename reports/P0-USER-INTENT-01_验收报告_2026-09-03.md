# P0-USER-INTENT-01 集成验收报告

**日期**：2026-09-03 18:30
**范围**：只做集成验收，不再改代码（Charles 第三阶段指令）
**被测链路**：用户口语买意 → Entity Resolver → user_watch_intents.json 落库 → daily_update 消费 → 报告「我的关注雷达」

---

## 一、6 项 literal 集成检查

| # | 检查项 | 结果 | 证据 |
|---|--------|------|------|
| 1 | daily_update 正常完成 | ✅ | EXIT 0；报告 `Ashare/reports/2026-09-03.html`(18:18)；P0-1 决策快照一致闸门 PASS |
| 2 | 今日 HTML 出现「我的关注雷达」 | ✅ | 注入成功，`{{WATCH_RADAR}}` 占位符 0 残留 |
| 3 | 000582 出现且只有 1 次 | ✅ | 雷达 tbody 仅 1 行 000582 |
| 4 | 显示 BUY_INTENT / TRACKING | ✅ | 「建仓关注 / BUY_INTENT / TRACKING / 尚未进入正式决策池」 |
| 5 | 显示真实行情，非测试数据 | ⚠️ 字面过 | 真实数据 latest_price=10.82 / +3.84%，但**底层是 08-26 缓存**（见第二节） |
| 6 | decision_feed 仍无 000582 + 原回归不受影响 | ✅ | decision_feed 91 候选无 000582（generated_at 17:10:40）；Gree regression **6/0**；契约测试 5fail/4pass 是既定红灯基线（P0 独立域，未触碰） |

---

## 二、升级验收链（Charles 自己设的更高 bar）—— 未全过

> WATCH_RADAR ↓ 000582 exists ↓ intent correct ↓ quote exists ↓ **quote freshness known** ↓ **stale cannot masquerade as today**

| 环节 | 结果 | 说明 |
|------|------|------|
| 000582 exists | ✅ | 雷达行存在 |
| intent correct | ✅ | BUY_INTENT / TRACKING 正确 |
| quote exists | ✅ | 显示 +3.84% |
| **quote freshness known** | ⚠️ **未满足** | 雷达行**无逐行数据日期标注**；全局页脚标「行情数据 2026-09-03（当日）」是 qm 主域日期，不适用于 000582 |
| **stale cannot masquerade as today** | ⚠️ **未满足** | 000582 不在今日 qm → 回退 `tencent_cache_2026-08-26.json`（08-26，距今 8 天）；读者会误读 +3.84% 为今日涨幅 |

**取证**：
- `Ashare/data/tencent/tencent_cache_2026-08-26.json` → 000582: `latest_price=10.82, change_pct=3.84`
- 今日**无**任何含 000582 的行情缓存（`Ashare/data/tencent/tencent_cache_2026-09-0*.json` 不存在）
- 报告页脚：`行情数据 2026-09-03（当日） · 决策契约 17:10` —— 该「当日」指 qm 主域（自选股池），000582 因不在 qm 而走缓存回退，但雷达行未标注其真实数据日期

---

## 三、根因归属（诚实边界）

- **P0-USER-INTENT-01 的核心交付已验证成立**：用户意图 → 持久化 → 雷达渲染，且**未污染 decision_feed / config.watchlist / 自动交易链**。
- 雷达「缺逐行新鲜度标注」是**既有报告级 quote 新鲜度标注缺陷**（整个报告的行情新鲜度标注本来就松散；000582 因不在今日 qm 而暴露），**不在 P0 原始 scope 内**。
- 但它**恰好是 Charles 升级验收要防的「陈数据冒充今日」情形**。

---

## 四、结论（Charles 选 B 已实施）

Charles 拍板 **(B)**：给雷达行加最小逐行新鲜度标注。已实施并复验通过。

**改动（定向、不碰架构）：**
- `Ashare/daily_update.py`：新增 `_tencent_cache_asof()`（返回缓存代表的数据日期）；`_build_watch_intent_radar` 按「报价来自 qm（当日）还是回退缓存（陈旧）」逐行打标。
- `Ashare/report_template.html`：新增 `.wr-stale` / `.wr-stale-cell` 样式（暗色 warn 色 + 左警示边）。

**复验结果（重跑 daily_update EXIT 0）：**
- 000582 雷达行：`+3.84%` + `<span class='wr-stale'>[数据 2026-08-26 · 非当日]</span>` + 悬停「该报价来自 2026-08-26 行情缓存，非今日实时数据（2026-09-03 无该标的行情刷新）；请勿当作今日涨跌」，单元格带警示左边界 ✅
- decision_feed：91 候选、000582 不在、generated_at 未变 ✅
- Gree regression：**6/0** ✅
- 当日 qm 域标的（若有）不会误标——`_from_qm` / `_tc_is_today` 双闸门保证仅「回退缓存且缓存≠今日」才标 ✅

**P0-USER-INTENT-01 正式关闭**：核心链路（意图→持久化→雷达，不污染候选池）已证 + 升级新鲜度链已满足（stale 不冒充今日）。未扩 scope（未碰 decision_feed / config.watchlist / 自动交易 / 晋升机制）。

> 注：底层 08-26 缓存陈旧是既有数据新鲜度缺陷（000582 不在今日 qm 故回退），本 P0 只做"诚实标注"不解决"无实时行情"本身——若要 000582 进当日 qm 需走行情刷新链（另一议题）。
